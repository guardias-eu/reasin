% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_species.R
\name{get_species}
\alias{get_species}
\title{Get species information from the EASIN's Catalogue Web Service}
\usage{
get_species(
  easin_id = NULL,
  scientific_name = NULL,
  environment = NULL,
  country_code = NULL,
  region_code = NULL,
  impact = NULL,
  taxon = NULL,
  taxonomy = NULL,
  present_in_country = NULL,
  status = NULL,
  horizon = NULL,
  partly_native = NULL,
  native_in_country = NULL,
  union_concern = NULL
)
}
\arguments{
\item{easin_id}{Integer. EASIN Species ID(s).}

\item{scientific_name}{Character. Scientific name(s) or part(s) of it. Case
insensitive.}

\item{environment}{Character. Environment type(s): one or more of: \code{"MAR"},
\code{"FRW"}, \code{"TER"}, \code{"OLI"} to filter species by, marine, freshwater,
terrestrial or oligohaline environments respectively. Use \code{environments()}
to look up the list of environment codes. Source: EASIN \href{https://easin.jrc.ec.europa.eu/apixg}{Catalogue Web Service} documentation.}

\item{country_code}{Character. Countries' ISO 3166-1 alpha-2 code(s) to
filter species of Member State concern. Use \code{countries()} to look up the
list of country codes. Source: EASIN \href{https://easin.jrc.ec.europa.eu/apixg}{Catalogue Web Service} documentation. Only few
states submitted their species of Member State concern to EASIN.}

\item{region_code}{Character. Species of Outermost regions concern codes as
defined in NUTS (Nomenclature of territorial units for statistics). Use
\code{regions()} to look up the list of region codes. Source: EASIN \href{https://easin.jrc.ec.europa.eu/apixg}{Catalogue Web Service} documentation.}

\item{impact}{Character. Species impact(s). One or more of: \code{"hi"} (high)
and \code{"lo"} (low).  Use \code{impacts()} to look up the list of impact codes and
their meaning. Source: EASIN \href{https://easin.jrc.ec.europa.eu/apixg}{Catalogue Web Service} documentation.}

\item{taxon}{Character named vector with the taxon name(s)
named by their taxonomic rank(s). Use \code{ranks()} to look up the list of valid
ranks. Source: EASIN \href{https://easin.jrc.ec.europa.eu/apixg}{Catalogue Web Service} documentation.}

\item{taxonomy}{Character named vector with the taxonomic names named
by their taxonomic rank. Provide them in the right order from kingdom up to
family. Source: EASIN \href{https://easin.jrc.ec.europa.eu/apixg}{Catalogue Web Service} documentation.}

\item{present_in_country}{Character. One or more countries' ISO 3166-1
alpha-2 codes to filter species present in these countries. Use
\code{countries()} to look up the list of country codes. Source: EASIN
\href{https://easin.jrc.ec.europa.eu/apixg}{Catalogue Web Service}
documentation.}

\item{status}{Character. Species status code(s). One or more of:  \code{"A"},
\code{"C"} and \code{"Q"}. Use \code{statuses()} to look up the list of status codes and
their meaning. Source: EASIN \href{https://easin.jrc.ec.europa.eu/apixg}{Catalogue Web Service} documentation.}

\item{horizon}{Logical. If \code{TRUE}, returns only species coming from Horizon
Scanning assessments. Only \code{TRUE} is allowed.}

\item{partly_native}{Logical. If \code{TRUE}, returns only specise which are
native in one or more EU countries.}

\item{native_in_country}{Character. One or more countries' ISO 3166-1 alpha-2
codes to filter species native in those countries. Use \code{countries()} to
look up the list of country codes. Source: EASIN \href{https://easin.jrc.ec.europa.eu/apixg}{Catalogue Web Service} documentation.}

\item{union_concern}{Logical. If \code{TRUE}, returns only species of Union
concern. Only \code{TRUE} is allowed.}
}
\value{
A tibble data frame containing species information.
}
\description{
This function retrieves species information from the EASIN's
\href{https://easin.jrc.ec.europa.eu/easin/Catalogue}{Catalogue}. Users can
retrieve records by species’ scientific name, environment, impact, taxonomy,
Union concern status
(\href{https://easin.jrc.ec.europa.eu/easin/LegalFramework/Index}{LegalFramework}). More on \href{https://easin.jrc.ec.europa.eu/apixg}{EASIN Web Services}.
}
\examples{
# Get list of all species in the EASIN catalogue
get_species()

# Get list of all species of Union concern
get_species(union_concern = TRUE)

# Get Horizon scanning species
get_species(horizon = TRUE)

# Get info about one or more species by EASIN Species IDs
get_species(easin_id = c("R00460", "R12250"))

# Get info about one or more species by scientific names or parts of it
get_species(scientific_name = c("Aceria ambrosia", "Procambarus"))

# Get species by `environment`
get_species(environment = c("MAR","OLI"))

# Get species by `country_code`
get_species(country_code = c("IE", "LT"))

# Get species by `region_code`
get_species(region_code = c("ES7", "PT3"))

# Get species by `taxon`
get_species(taxon = c(family = "Vespidae"))

# Get species by full `taxonomy` levels (up to family)
get_species(
  taxonomy = c(
    kingdom = "Animalia",
    phylum = "Arthropoda",
    class = "Insecta",
    order = "Hymenoptera",
    family = "Vespidae"
  )
)

# Get species present in one or more countries
get_species(present_in_country = c("LU", "IE"))

# Get species by `status`
get_species(status = c("Q", "A"))

# Get species which are native in at least one country
get_species(partly_native = TRUE)

# Get species which are native in one or more countries
get_species(native_in_country = c("EE","FI"))
}
\concept{species functions}
